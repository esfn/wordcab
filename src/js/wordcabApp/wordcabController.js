(function(window) {
    'use strict';

    const wordcabController = {}

    console.log('wordcabController init');

    window.app = window.app || {};
    window.app.wordcabController = wordcabController;
})(window);